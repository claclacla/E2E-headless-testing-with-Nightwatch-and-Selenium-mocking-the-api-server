import React from 'react';
import PropTypes from 'prop-types';

import HomeContainerView from './View';

const HomeContainer = function ({ getProducts }) {
  return (
    <HomeContainerView />
  );
}

export default HomeContainer